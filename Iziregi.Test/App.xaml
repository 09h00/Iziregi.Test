<!-- File: App.xaml -->
<Application x:Class="Iziregi.Test.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">

    <Application.Resources>

        <!-- Cellules texte de DataGrid : lisible + ellipsis + tooltip -->
        <Style x:Key="IziregiGridTextCell" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="6,0,6,0"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <!-- Cellules DataGrid (optionnel) : hauteur stable -->
        <Style x:Key="IziregiGridCell" TargetType="{x:Type DataGridCell}">
            <Setter Property="Padding" Value="2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- DataGrid par défaut (optionnel) -->
        <Style x:Key="IziregiGrid" TargetType="{x:Type DataGrid}">
            <Setter Property="CellStyle" Value="{StaticResource IziregiGridCell}"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="CanUserReorderColumns" Value="True"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserSortColumns" Value="True"/>
        </Style>

    </Application.Resources>
</Application>