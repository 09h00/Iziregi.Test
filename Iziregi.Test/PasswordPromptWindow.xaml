<!-- File: PasswordPromptWindow.xaml -->
<Window x:Class="Iziregi.Test.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dossier verrouillé"
        Width="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Arial"
        Background="{StaticResource PageBackgroundBrush}">

    <Window.Resources>
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   Grid.Row="0"
                   Text="Dossier verrouillé"
                   FontSize="17"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,4"/>

        <TextBlock x:Name="MessageTextBlock"
                   Grid.Row="1"
                   Text="Ce dossier est protégé par un mot de passe."
                   FontSize="12"
                   Foreground="#6B7280"
                   TextWrapping="Wrap"
                   Margin="0,0,0,14"/>

        <PasswordBox x:Name="PasswordInputBox"
                     Grid.Row="2"
                     Height="34"
                     Padding="8,4"
                     BorderBrush="#D1D5DB"
                     BorderThickness="1"
                     Background="White"
                     VerticalContentAlignment="Center"
                     KeyDown="PasswordInputBox_KeyDown"/>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="3"
                   Text="Mot de passe incorrect."
                   FontSize="12"
                   Foreground="#B91C1C"
                   Visibility="Collapsed"
                   Margin="0,8,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource CancelButtonStyle}" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="UnlockButton" Content="Déverrouiller" Style="{StaticResource PrimaryButtonStyle}" Click="UnlockButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
