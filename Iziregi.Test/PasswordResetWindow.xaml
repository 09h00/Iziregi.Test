<!-- File: PasswordResetWindow.xaml -->
<Window x:Class="Iziregi.Test.PasswordResetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Réinitialiser le mot de passe Directeur"
        Width="380"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Arial"
        Background="{StaticResource PageBackgroundBrush}">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Réinitialiser le mot de passe Directeur"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   TextWrapping="Wrap"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   Text="Un code à 6 chiffres a été envoyé par email à l'adresse de contact enregistrée pour ton bureau. Il est valable 30 minutes."
                   FontSize="12"
                   Foreground="#6B7280"
                   TextWrapping="Wrap"
                   Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="Code reçu par email" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="CodeTextBox"
                 Grid.Row="3"
                 Height="34"
                 Padding="8,4"
                 BorderBrush="#D1D5DB"
                 BorderThickness="1"
                 Background="White"
                 VerticalContentAlignment="Center"
                 MaxLength="6"
                 Margin="0,0,0,12"/>

        <TextBlock Grid.Row="4" Text="Nouveau mot de passe Directeur" Style="{StaticResource LabelStyle}"/>
        <PasswordBox x:Name="NewPasswordBox"
                     Grid.Row="5"
                     Height="34"
                     Padding="8,4"
                     BorderBrush="#D1D5DB"
                     BorderThickness="1"
                     Background="White"
                     VerticalContentAlignment="Center"
                     Margin="0,0,0,12"/>

        <TextBlock Grid.Row="6" Text="Confirmer le mot de passe" Style="{StaticResource LabelStyle}"/>
        <PasswordBox x:Name="ConfirmPasswordBox"
                     Grid.Row="7"
                     Height="34"
                     Padding="8,4"
                     BorderBrush="#D1D5DB"
                     BorderThickness="1"
                     Background="White"
                     VerticalContentAlignment="Center"
                     Margin="0,0,0,4"/>

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="8"
                   Text=""
                   FontSize="12"
                   Foreground="#B91C1C"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Margin="0,4,0,0"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource CancelButtonStyle}" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button x:Name="SubmitButton" Content="Réinitialiser" Style="{StaticResource PrimaryButtonStyle}" Click="SubmitButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
