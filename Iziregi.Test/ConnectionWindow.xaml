<!-- File: ConnectionWindow.xaml -->
<!-- ✅ NOUVEAU (18.08.2026, demande de Joe) : extrait de la page Paramètres > Admin (section
     "Connexion au serveur Iziregi") en fenêtre séparée. -->
<Window x:Class="Iziregi.Test.ConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connexion au serveur Iziregi"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Arial"
        Background="{StaticResource PageBackgroundBrush}">

    <Window.Resources>
        <!-- ✅ Modèle propre, pas BasedOn PrimaryButtonStyle (18.08.2026, correction demandée
             par Joe, "le bouton Fermer est toujours le même au survol") : PrimaryButtonStyle
             force un survol bleu directement sur la bordure via son propre ControlTemplate.Trigger,
             qui écrasait toute tentative de le changer depuis un style dérivé. -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Connexion au serveur Iziregi"
                   FontSize="17"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,14"/>

        <TextBlock Grid.Row="1" Text="URL du serveur" FontSize="13" Foreground="#6B7280" Margin="0,0,0,4"/>
        <TextBox x:Name="ServerUrlTextBox" Grid.Row="2" Style="{StaticResource InputStyle}"/>

        <TextBlock Grid.Row="3" Text="Clé d'accès" FontSize="13" Foreground="#6B7280" Margin="0,0,0,4"/>
        <TextBox x:Name="ServerKeyTextBox" Grid.Row="4" Style="{StaticResource InputStyle}"/>

        <TextBlock Grid.Row="5"
                   Text="La clé d'accès vous est fournie par votre administrateur Iziregi."
                   FontSize="13"
                   Foreground="#6B7280"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal">
            <Button Content="Tester" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="TestConnection_Click"/>
            <Button Content="Enregistrer" Style="{StaticResource PrimaryButtonStyle}" Click="SaveConnection_Click"/>
        </StackPanel>

        <TextBlock x:Name="ConnectionStatusText" Grid.Row="7" Margin="0,10,0,0" FontSize="13" TextWrapping="Wrap" Visibility="Collapsed"/>
    </Grid>
</Window>
